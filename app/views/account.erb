<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">  
  </head>
  <body id="page-top">
    <% if Helpers.is_logged_in?(session) == true %>
      <h2><%= Helpers.current_user(session).username %></h2>
      <h2><%= Helpers.current_user(session).balance %></h2>
    <% end %>
  </body>
</html>